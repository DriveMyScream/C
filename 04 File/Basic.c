#include <stdio.h>
int main() {
    FILE *ptr;
    ptr = fopen("simple.txt", "r");  // for read a data form file
    // ptr = fopen("simple.txt","w");   // for write a data form file
return 0;
}